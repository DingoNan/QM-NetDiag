# 网络自检工具（NetDiag）设计文档 v1.1

> 版本：v1.1 ｜ 日期：2026-08-13
> 适用场景：国产化信创网络（一体化系统连接不稳定、断连、访问缓慢）

---

## 一、项目定位

**一句话**：面向一线用户的"傻瓜式"网络体检工具 —— 双击打开、点一下"开始体检"、自动跑完所有测试、一键导出报告发给运维。

**核心价值**：把"网络是不是有问题、问题出在哪"这个专业判断，变成任何人都会操作的自助流程，替代现在的手工 bat/sh 脚本，降低运维沟通成本、缩短排障时间。

**目标用户**：
- 一线财政人员（非技术）：只会点按钮，需要大字号、明确指引、结果一眼看懂
- 运维同事（报告接收方）：需要结构化、可定位问题的详细数据

---

## 二、平台矩阵（6 个绿色包）

| 平台包 | 系统 | CPU 架构 | iperf3 来源 | 打包环境 |
|---|---|---|---|---|
| `网络自检工具_Win32` | Windows 7/10/11 | x86 (32位) | `iperf3.1.4_32.zip` ✅已就位 | Windows x86 或 x64 机器 |
| `网络自检工具_Win64` | Windows 7/10/11 | x86_64 | `iperf3.1.4_64.zip` ✅已就位 | Windows x64 机器 |
| `网络自检工具_麒麟x64` | 麒麟 V10 / 统信 UOS | x86_64 | 源码包编译（iperf-3.14.tar.gz） | 麒麟 x64 机器 |
| `网络自检工具_麒麟ARM` | 麒麟 V10（鲲鹏/飞腾） | aarch64 | 源码包编译（iperf-3.14.tar.gz） | 麒麟 ARM 机器（如 67.133 服务器） |
| `网络自检工具_MacIntel` | macOS 10.15+ | x86_64 | brew 安装或源码编译 | macOS Intel 机器 |
| `网络自检工具_MacApple` | macOS 11+ | arm64 (M 系列) | brew 安装或源码编译 | macOS Apple Silicon 机器 |

**自动识别机制**：
1. 每个绿色包内置 `system_check`（启动时自检）：比对当前系统 + CPU 架构与包类型，不一致时弹出明确提示"当前系统为 xx，请使用 xx 版本"，并自动识别系统信息写入报告头
2. U 盘分发时提供**智能启动器**（放 U 盘根目录）：
   - Windows：`自动选择版本.bat`（按 `%PROCESSOR_ARCHITECTURE%` 判断）
   - 麒麟/macOS：`自动选择版本.sh`（按 `uname -m` 判断）
   - 自动定位并启动对应版本，用户完全无感

> 注：macOS 有安全策略（Gatekeeper），首次运行需"右键 → 打开"或 `xattr -d com.apple.quarantine`，需在 README 中说明。

---

## 三、测试项设计（7 项）

| # | 测试项 | 默认目标 | 关键指标 | 判定规则 |
|---|---|---|---|---|
| 1 | Ping 连通性 | 59.211.236.211 | 丢包率、延迟 min/avg/max/mdev | 丢包 0% 绿；<5% 黄；≥5% 红 |
| 2 | TCP 端口探测 | 59.211.236.211:30014 | 连通性、建连耗时 | 通/不通 |
| 3 | iperf3 单流带宽 | 同上 | 带宽 Mbps | 与基线比（可配置参考值） |
| 4 | iperf3 并行 8 流 | 同上 | 带宽 Mbps | 与基线比 |
| 5 | iperf3 反向（下行） | 同上 | 带宽 Mbps、重传 retr | 重传率 >2% 黄，>5% 红 |
| 6 | 路由追踪 Tracert | 59.211.236.211 | 逐跳延迟、断点定位 | 超时/丢包跳标红 |
| 7 | 长时监测 | 目标地址 | 不稳定事件时间线 | 事件化记录 |

**增强测试（高级模式）**：
- DNS 解析测试（一体化系统域名）
- HTTP 探测（一体化系统首页响应时间，模拟真实"系统缓慢"体感）
- 多目标对比测试（内网 67.133 vs 外网映射 59.211.236.211，判断问题出在内网还是出口链路）

**监测模式（针对"时断时续"场景的核心功能）**：
- 每 30s（可调）轮询 Ping + TCP 探测
- 持续 10 分钟 / 1 小时 / 自定义时长
- 自动记录事件：丢包事件、断连事件、延迟突增事件（>3 倍基线）
- 输出**不稳定事件时间线**：几点几分发生了什么，运维一眼定位

---

## 四、技术方案

**技术栈**：Python 3.8+（兼容麒麟 V10 自带 python3）+ tkinter（内置 GUI）+ PyInstaller（打包）

| 模块 | 实现方式 |
|---|---|
| GUI | tkinter + ttk 定制样式（统一圆角卡片、主题色、SVG 风格图标） |
| ping/tracert | 调用系统命令（win: ping -n / tracert；linux/mac: ping -c / traceroute）并解析 |
| iperf3 | 调用内置二进制（tools/对应平台/iperf3.exe 或 iperf3），解析 JSON 输出（-J） |
| 报告 | HTML 模板（内嵌 CSS，浏览器打开可打印 PDF）+ TXT 日志 |
| 剪贴板 | tkinter clipboard（复制摘要，微信直发） |
| 打包 | PyInstaller onedir 模式（降低杀软误报），每平台在对应系统上打包 |

**关键注意事项**：
1. Windows iperf3 是 cygwin 版，`iperf3.exe` 必须与 `cygwin1.dll` 同目录（tools/ 已按此组织）
2. 麒麟 ARM 打包必须在 ARM 机器上进行（PyInstaller 不跨架构）；需先 `yum install python3-tkinter`
3. 报告文件统一 UTF-8 写入，避免 Windows GBK 乱码
4. 杀软误报：onedir 模式 + 首次运行说明；建议后续加代码签名
5. macOS：打包后在真机验证 Gatekeeper 策略
6. iperf3 输出统一加 `-J`（JSON），解析更稳，不依赖英文/中文区域差异

---

## 五、UI 设计（重点）

### 5.1 设计原则

1. **一步到位**：默认"快速体检"，一个主按钮完成所有操作；高级功能折叠
2. **状态可视化**：全程用绿/黄/红三色 + 图标传达状态，用户无需读文字
3. **大字号大按钮**：面向 40+ 岁办公人群，主按钮 ≥ 120px，正文 ≥ 14px
4. **离线可用**：所有资源内嵌，无外网依赖，图标用内嵌 SVG/Unicode
5. **结果即行动**：报告页直接给出"接下来该怎么做"的建议

### 5.2 视觉规范

| 元素 | 规范 |
|---|---|
| 主色 | 政务蓝 `#1B5E9E`（顶部横幅、主按钮） |
| 强调色 | 亮蓝 `#2196F3`（进度、选中态） |
| 成功色 | 绿 `#2E9E5B` |
| 警告色 | 橙 `#E6A23C` |
| 错误色 | 红 `#D64545` |
| 背景 | 浅灰蓝 `#F5F7FA` |
| 卡片 | 白底、圆角 12px、轻阴影 |
| 字体 | 微软雅黑（Windows）/ PingFang SC（macOS）/ 文泉驿（麒麟） |
| 主窗口 | 960×680，可缩放，最小 800×600 |

### 5.3 页面结构（6 个界面）

**① 主界面（快速体检）**

```
┌──────────────────────────────────────────────┐
│ ▣ 网络自检工具 v1.0      [设置] [帮助] │ ← 深蓝横幅
├──────────────────────────────────────────────┤
│  目标服务器  ┌────────────────────────────┐   │
│             │ 🖥 59.211.236.211:30014     │   │
│             │ ● 服务器在线（上次探测正常） │   │
│             └────────────────────────────┘   │
│                                              │
│           ┌──────────────────┐               │
│           │   ▶ 开始体检      │               │ ← 巨型主按钮
│           └──────────────────┘               │
│   点击后自动完成 7 项测试，约 3 分钟           │
│                                              │
│  测试项目：                                   │
│  ✓ Ping 连通性     ✓ TCP 端口      ✓ 单流带宽 │
│  ✓ 8流带宽        ✓ 反向带宽      ✓ 路由追踪 │
│  ✓ 长时监测（可选）                            │
├──────────────────────────────────────────────┤
│ [快速体检]  [高级模式]  [长期监测]  [历史报告] │ ← 底部导航
└──────────────────────────────────────────────┘
```

**② 体检进度界面**：中央大进度环（百分比）+ 当前项动画提示 + 逐项状态列表（✓完成 / ⚡进行中 / ○等待）

**③ 报告界面**：
- 顶部结论横幅：🟢 链路基本正常 / 🟡 存在轻微异常 / 🔴 建议报修
- 6 个指标卡片：平均延迟 / 丢包率 / 下行带宽 / 上行带宽 / 重传率 / 稳定性评级
- 详细测试表（可展开）+ 监测时间线（如有）
- 底部操作栏：`保存报告` `复制摘要` `浏览器打开` `重新测试`

**④ 高级模式**：参数表单（ping 次数、iperf 时长/流数/双向、目标地址、单跑某项）+ 自定义目标管理

**⑤ 设置界面**：服务器地址/端口、一体化系统地址、默认参数、报告保存目录、恢复默认

**⑥ 监测模式**：时长选择、间隔设置、实时迷你图表、不稳定事件列表

### 5.4 易用性细节

- 所有按钮有悬停/按下态反馈；测试中禁止重复点击（防呆）
- 每个测试项都有"这是什么"悬停提示（tooltip）
- 报告摘要为纯文本 + emoji，微信直接可读：
  `📊 网络自检报告 08-13 15:30 ｜ 目标 59.211.236.211:30014 ｜ 丢包0% 延迟12ms 下行48M 上行23M 重传0.2% ｜ 结论：链路正常`
- 异常时按钮变红并弹"建议先检查网线/换网络再测一次"的引导
- 支持拖拽窗口、Esc 关闭弹窗等常规交互

---

## 六、报告设计

**报告头**：测试时间 / 客户端信息（机器名、系统版本、架构、本机 IP）/ 目标服务器 / 报告生成工具版本
**概要表**：7 项测试 ✅/⚠️/❌ + 关键数字
**详细数据**：ping 统计、iperf3 每次结果（带宽/重传/窗口）、tracert 逐跳（异常标红）
**监测时间线**（如开启）：事件列表 + 时间段
**结论建议**（自动生成规则）：
- 全绿 → "链路基本正常；如仍感觉系统缓慢，建议检查一体化系统服务器端资源"
- 丢包 ≥5% / 重传 >5% / 带宽低于参考值 → "存在明显链路质量问题，建议联系运营商报修，可出示本报告"
- 断连事件 → "xx:xx 发生断连，建议重点排查该时段出口链路"
- 多目标对比 → "内网正常但外网异常 → 问题在出口/运营商链路"

**文件命名**：`网络测试报告_机器名_YYYYMMDD_HHMMSS.html` + 同名 `.txt` 日志
**存储位置**：默认程序旁 `结果/` 目录，可在设置中修改

---

## 七、配置设计

`config.ini`（程序旁，运维可改后整体分发）：
```ini
[目标]
外网映射地址 = 59.211.236.211
外网映射端口 = 30014
内网真实地址 = 192.168.67.133
内网真实端口 = 5201
一体化系统地址 =
参考带宽Mbps = 50

[测试参数]
ping次数 = 50
iperf时长秒 = 30
并行流数 = 8
监测间隔秒 = 30
报告目录 = 结果

[外观]
主题 = 政务蓝
```

---

## 八、项目目录结构

```
网络测试工具/
├── README.md                    # 使用说明（分平台）
├── 设计文档.md                  # 本文档
├── src/
│   ├── main.py                  # 入口（含系统自检）
│   ├── config.py                # 配置读写
│   ├── platform_info.py         # 系统/架构识别
│   ├── core/
│   │   ├── base_test.py         # 测试基类（超时/日志/结果结构）
│   │   ├── ping_test.py
│   │   ├── tcp_test.py
│   │   ├── iperf3_test.py       # JSON 解析
│   │   ├── traceroute_test.py
│   │   ├── dns_test.py
│   │   └── http_probe.py
│   ├── monitor/
│   │   └── monitor.py           # 长时监测 + 事件时间线
│   ├── report/
│   │   ├── html_report.py
│   │   ├── text_report.py
│   │   └── templates/report.html
│   └── ui/
│       ├── main_window.py       # 主界面/进度/报告
│       ├── advanced_window.py   # 高级模式
│       ├── settings_window.py   # 设置
│       └── monitor_window.py    # 监测模式
├── tools/                       # 各平台 iperf3（已解压 win32/win64）
├── docs/prototype/              # HTML 高保真原型
├── config.ini
├── build_win.bat                # Windows 打包
├── build_kylin.sh               # 麒麟打包（含 iperf3 编译）
├── build_macos.sh               # macOS 打包
└── dist/                        # 打包产物（6 个绿色包）
```

---

## 九、实施计划

| 阶段 | 内容 | 验收标准 |
|---|---|---|
| P0 | ✅ 目录结构 + iperf3 就位 | tools/ 有 6 平台 iperf3 |
| P1 | UI 原型（HTML 高保真）+ 设计评审 | 原型可点击演示，用户确认样式 |
| P2 | Windows x64 版完整功能（7 项测试 + 报告） | Win64 包可用 |
| P3 | Win32 + 麒麟 x64/ARM 打包验证 | 3 平台包通过 |
| P4 | macOS Intel/Apple 打包 + Gatekeeper 处理 | 2 平台包通过 |
| P5 | 高级功能（监测、多目标、HTTP/DNS）+ 报告美化 | 全功能发布版 |

---

## 十、风险与建议

1. **杀软误报**：PyInstaller 打包易被 360/火绒拦截 → onedir 模式 + 白名单说明 + 可选代码签名
2. **麒麟打包**：PyInstaller 不跨架构 → ARM 包在 67.133 服务器现场打；麒麟需装 `python3-tkinter`
3. **macOS 安全**：Gatekeeper 拦截 → README 写明"右键打开"；可选签名/公证
4. **NAT 链路说明**：带宽测试走公网映射，结果受运营商链路影响 → 报告自动注明
5. **隐私边界**：纯本地生成、不上传任何数据；报告仅含排障所需信息（本机 IP 用于定位）
6. **数据资产**：报告按"机器名+日期"归档，可沉淀为全区网络质量台账，为后续"问题区域画像"提供数据
7. **长期演进**：可选 Web 版监测端（运维侧汇总所有用户报告的可视化看板）——二期规划

---

## 附：一期功能范围（本次开发）

- ✅ 6 平台绿色包构建
- ✅ 快速检测（Ping/TCP/iperf3 三档/路由/DNS/HTTP）+ 高级探测 + 设置
- ✅ HTML/TXT 报告 + 摘要一键复制 + 报告类型分类（快速检测/高级探测/长期监测）
- ✅ 配置化目标地址（含独立 iperf3 服务器地址/端口）
- ✅ 系统自检与智能启动器 + 残留进程检测
- ✅ 长时监测：自动停止/开始时间/事件时间线/HTTP 探测/趋势图
- ✅ HTTP/DNS/多目标对比

---

## 版本演进记录

### v1.3（2026-08-17）—— 专业能力增强（并行/基线/出口/定时/网卡/TLS）

- **测试并行化**：iperf3 三档与路由追踪并行执行（最多 3 并发），整体检测耗时压缩 50%+；快项（Ping/TCP/DNS/HTTP）保留依赖跳过链
- **历史基线对比**：报告自动与同类型上次检测对比（上行/下行带宽、延迟、丢包变化百分比），HTML/TXT 均展示"较上次检测"
- **出口专项检测**：公网目标（默认 223.5.5.5:53）连通性探测，辅助区分内网/出口问题；政务网隔离公网时可关闭
- **定时自动检测**：设置页或 config.ini 配置启用+时间，到点自动跑快速检测并静默保存报告
- **多网卡选择**：指定测试源 IP（Ping -S/-I、iperf3 -B、TCP socket bind），多网卡机器可精确选网卡
- **TLS 证书深度检测**：HTTPS 探测显示证书有效期至（纯标准库 DER 解析，自签名证书同样可读）
- 测试项构造抽取为 `_build_tests()`，便于维护与自动化验证

### v1.2（2026-08-17）—— 诊断智能化与体验全面升级

**诊断能力（借鉴 Network Doctor 分层思想）**
- 分层诊断结论：报告自动定位问题层级（DNS 配置 / 本地网络 / 目标服务端 / 带宽限制 / 链路稳定性）
- 依赖跳过：DNS 解析失败自动跳过 HTTP 探测，避免无意义测试
- 修复提示（💡 建议）：每项异常附"下一步怎么做"，不懂网络也能操作
- 禁 ping 自动识别：Ping 全超时 + TCP 交叉验证，区分"禁 ping"与"真不可达"

**带宽评估**
- 取消预设参考带宽依赖（留空=自动评估），实测自动给出带宽估算与通俗等级（百兆级/较快/普通办公/偏慢/很慢/极慢）

**长期监测增强**
- 轮询项扩充：Ping（禁 ping 跳过）+ TCP（建连耗时）+ HTTP（响应耗时，可选）
- 监测趋势：SVG 折线图（Ping/TCP/HTTP 耗时随时间变化，断连标红叉）
- 统计口径修正：禁 ping 但 TCP 通不再误计为异常
- 异常终止识别：区分"到时自动停止"与"线程异常终止"，不再误报/卡死

**交互与体验**
- 测试运行中可随时回进度页（顶部状态/首页按钮/导航三入口）
- 一键停止测试（0.2s 内强制结束，生成"已中止"报告）
- 服务器状态真实探测（多目标：映射端口/iperf3/一体化系统 HTTP），不再写死"在线"
- 设置保存即生效（无需重启），监测面板/首页同步刷新
- 长期监测时间冻结（停止后不再增长）、开始时间/已运行/剩余显示
- Tracert 提速约 4 倍（-w 1000 + 动态超时上限）
- HTTPS 自签名证书自动放行（政务内网场景）

**品牌与文档**
- 工具更名：网络自检工具（去除"财政政务"，"广西信创"→"国产化信创"）
- 政务蓝信号图标：exe / 窗口 / 界面 / 报告
- 设计署名：浅木·先生（界面横幅 + 报告页脚）
- 多目标配置：外网映射 / iperf3 服务器 / 一体化系统地址分离
